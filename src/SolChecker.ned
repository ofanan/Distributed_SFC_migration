package src;

//
// Check the feasibility of the solution, calculate its cost, and prints the relevant data to files.
//

simple SolChecker
{
    parameters:
        @display("i=misc/node_vs,gold");
        
}